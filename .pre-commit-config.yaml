repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: ruff check
        entry: uv run --extra dev ruff check --force-exclude
        language: system
        types_or: [python, pyi]
