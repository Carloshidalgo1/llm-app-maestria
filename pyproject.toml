[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "carnicos-kb"
version = "0.1.0"
description = "Pipeline de scraping, extraccion PDF y chunking para una base de conocimiento de Alimentos Carnicos."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12",
    "docling>=2.90",
    "langchain>=1.2",
    "langchain-openai>=0.1",
    "langchain-postgres>=0.0.12",
    "langchain-text-splitters>=0.3",
    "langsmith>=0.1",
    "langgraph>=1.0",
    "langgraph-checkpoint-postgres>=2.0",
    "psycopg[binary]>=3.1",
    "lxml>=5.0",
    "openai>=1.0",
    "pymupdf>=1.24",
    "pypdfium2>=4.30",
    "python-dotenv>=1.0",
    "requests>=2.32",
    "streamlit>=1.38",
    "trafilatura>=1.12",
    "urllib3>=2.2",
]

[project.optional-dependencies]
dev = [
    "pre-commit>=4.6.0",
    "pytest>=8.0",
    "ruff>=0.8",
]

[project.scripts]
carnicos-chunk = "carnicos_kb.chunking:main"
carnicos-pdf = "carnicos_kb.pdf_extractor:main"
carnicos-pdf-fast = "carnicos_kb.pdf_text_extractor:main"
carnicos-scrape = "carnicos_kb.scraper:main"
carnicos-qa = "carnicos_kb.qa_system:main"
carnicos-app = "carnicos_kb.streamlit_runner:main"
carnicos-validate-mod1 = "carnicos_kb.validate_module1:main"
carnicos-build-rag = "carnicos_kb.rag_index_builder:main"

[tool.hatch.build.targets.wheel]
packages = ["src/carnicos_kb"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
tmp_path_retention_policy = "failed"
addopts = "--basetemp=.pytest_tmp"

[tool.ruff]
line-length = 100
target-version = "py310"
